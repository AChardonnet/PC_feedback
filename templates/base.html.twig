<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Feedback - {% block title %}{% endblock %}</title>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    <link rel="stylesheet" href="https://bootswatch.com/5/flatly/bootstrap.css">
    {% block stylesheets %}
    {% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href=" {{ path('home_home') }} ">Feedback</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('course_courses') }}">Liste des cours</a>
                </li>
                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('security_signup') }}" class="nav-link">Inscription</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('security_login') }}" class="nav-link">Connexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('security_logout') }}" class="nav-link">Déconnexion</a>
                    </li>
                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item"><a href="{{ path('admin_dashboard') }}" class="nav-link">Panneau d'administration</a></li>
                    <li><a href="{{ path('admin_list') }}" class="nav-link">Liste des Admins</a></li>
                    <li><a href="{{ path('admin_listM') }}" class="nav-link">Liste des Modérateurs</a></li>
                    <li><a href="{{ path('admin_promote') }}" class="nav-link">Ajouter un nouvel Admin</a></li>
                    <li><a href="{{ path('admin_newModerator') }}" class="nav-link">Ajouter un nouveau Modérateur</a></li>
                {% endif %}
                {% if is_granted("ROLE_MODERATOR") %}
                    <li class="nav-item">
                        <a href="{{ path('moderator_feedbacks') }}" class="nav-link">Commentaires à valider</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
<script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
</body>
</html>
